The GUI shall:
    1. Be able to specify the pattern style
        a. Will be using a menu
    2. Be able to specify the size of the pattern
        a. Will scale the pattern by specifiying the size of the "box" (operational area)
    3. Be able to specify the speed of the drone
        a. This will be constant for all segments of the drone
        b. Will need to specify a minimum and maximum speed (0.1 m/s - 2 m/s)
    4. Be able to display the planned flight path with dimensions
    5. Be able to display the actual path that is captured with the BitCraze positioning system
        a. Needs to be be a differnt color to allow for the two paths to be easily discernible
    6. 
